export interface Roles {
    editor?: boolean;
    admin?: boolean;
  }

export interface CheckersInterface {
    checkerMail?: string;
    checkerName?: string;
    key?: string;
    userID?:string;
  }
  
  
export interface CheckerInterface {
  checkerMail?: string;
  checkerName?: string;
  key?: string;
  userID?: string;
}
